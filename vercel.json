{
  "buildCommand": "npm ci --prefer-offline --no-audit && npm run build",
  "installCommand": "npm ci --prefer-offline --include=optional",
  "rewrites": [
    { "source": "/((?!api/).*)", "destination": "/" }
  ]
}
